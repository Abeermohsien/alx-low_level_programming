#include <stdio.h>
/**
 * main - prints all the arguments with new line
 * @argc: int
 * @argv: char
 * Return: 0.
 */
int main(int argc, char *argv[])
{
int i;
while (arg--)
{
printf("%s\n", argv[i]);
i++;
}
return (0);
}
