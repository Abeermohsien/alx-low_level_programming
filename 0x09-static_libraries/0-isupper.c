#include "main.h"
#include <ctype.h>
/**
 * _isupper - check the upper case letters.
 * @c: is a character
 * Return: Always 0.
 */
int _isupper(int c)
{
if (isupper(c))
{
return (1);
}
return (0);
}
