#include "main.h"
#include <stdio.h>
/**
 * main - check the code
 *
 * Return: Always 0.
 */
void print_to_98(int n)
{
int i, start;
printf("enter anumber:");
scanf("%d", &start);
for (i = start; i <= 98; i++)
{
prinf("%d, ", i);
}
return (0);
}
