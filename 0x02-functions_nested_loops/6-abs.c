#include "main.h"

/**
 * _abs - computes the abslute value of numbers
 * @c: number
 *
 * Return: Always 0.
 */
int _abs(int c)
{
if (c < 0)
{
int absv;
absv = c * absv;
return (absv);
}
return (c);
}
